#include <stdio.h>
/*Pišemo softver za upravljanje nekretninama i želimo da označimo varijablu
 * koja:
 */
int main() {
  // Sadrži ukupnu površinu nekretnine
  float povrsinaNekretnine; // Tip varijable float jer je potrebna decimalna vrijednost

  // Sadrži broj vlasnika nekretnina
  int brojVlasnika; // Int jer oznacavamo cjelobrojni broj vlasnika

  // Prvo slovo prezimena glavnog vlasnika imovine
  char prezimeGlavnogVlasnika; // Uopšteno, pošto unosimo samo 1 slovo može se
                               // koristiti char

  // Označava da li se radi o građevinskoj parceli ili ne
  // bool gradjevinskaParcela;
  // Tip boolean jer je odgovor true ili false

  /*Objasnite Mađarsku Notaciju.

  Mađarska notacija je u biti stil imenovanja varijable. Npr. umjesto int
  brojVlasnika napisali bi intbrojVlasnika, gdje int oznacava tip varijable, a
  brojVlasnika varijablu. Služi kako bi lakše razumjeli i distinktovali tipove
  varijabli i njihovu svrhu, te umanjili moguće greške*/

  return 0;
}
